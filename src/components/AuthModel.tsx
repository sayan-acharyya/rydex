import React from 'react';

type PropType = {
    open: boolean;
    onClose: () => void;
};

const AuthModel = ({ open, onClose }: PropType) => {
    return (
        <div> </div>
    );
};

export default AuthModel;